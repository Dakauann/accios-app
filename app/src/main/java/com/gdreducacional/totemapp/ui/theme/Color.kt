package com.gdreducacional.totemapp.ui.theme

import androidx.compose.ui.graphics.Color

object AcciosColors {
    val gradientStart = Color(0xFF1F1C2C)
    val gradientEnd = Color(0xFF928DAB)

    val surface = Color(0xFF1B1A29)
    val surfaceOverlay = Color(0xFF1B1A29)
    val surfaceDeep = Color(0xFF0B1120)

    val success = Color(0xFF43A047)
    val error = Color(0xFFE53935)
    val errorLight = Color(0xFFFF8A80)
    val detecting = Color(0xFF42A5F5)

    val glowWarm = Color(0xFFFFECB3)
    val glowWarmFade = Color(0xFFFFF8E1)

    val glass = Color.White.copy(alpha = 0.14f)
    val glassBorder = Color.White.copy(alpha = 0.35f)
    val glassElevated = Color.Black.copy(alpha = 0.55f)
    val glassCard = Color.White.copy(alpha = 0.06f)
    val glassCardBorder = Color.White.copy(alpha = 0.08f)

    val textPrimary = Color.White
    val textSecondary = Color.White.copy(alpha = 0.85f)
    val textTertiary = Color.White.copy(alpha = 0.7f)
    val textDim = Color.White.copy(alpha = 0.65f)
    val textDisabled = Color.White.copy(alpha = 0.5f)

    val buttonGlass = Color.White.copy(alpha = 0.15f)

    val divider = Color.White.copy(alpha = 0.08f)
}